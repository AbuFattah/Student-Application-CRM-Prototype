
  # Student Application Workflow Prototype

  This is a code bundle for Student Application Workflow Prototype. The original project is available at https://www.figma.com/design/i8eniLg90iSHxnguLadbFZ/Student-Application-Workflow-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  